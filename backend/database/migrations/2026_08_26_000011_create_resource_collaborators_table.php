<?php
use Illuminate\Database\Migrations\Migration;use Illuminate\Database\Schema\Blueprint;use Illuminate\Support\Facades\DB;use Illuminate\Support\Facades\Schema;
return new class extends Migration{public function up():void{Schema::create('resource_collaborators',function(Blueprint $t){$t->id();$t->string('resource_type');$t->unsignedBigInteger('resource_id');$t->foreignId('user_id')->constrained()->cascadeOnDelete();$t->string('permission');$t->foreignId('granted_by')->nullable()->constrained('users')->nullOnDelete();$t->timestamps();$t->unique(['resource_type','resource_id','user_id']);$t->index(['user_id','resource_type']);});$now=now();DB::table('device_templates')->whereNotNull('created_by')->orderBy('id')->each(function($template)use($now):void{DB::table('resource_collaborators')->insertOrIgnore(['resource_type'=>'device_template','resource_id'=>$template->id,'user_id'=>$template->created_by,'permission'=>'edit','granted_by'=>$template->created_by,'created_at'=>$now,'updated_at'=>$now]);});}public function down():void{Schema::dropIfExists('resource_collaborators');}};
