import type {ReactNode} from "react";import {Card,CardContent} from "../../ui";export default function KpiCard({label,value,detail}:{label:string;value:ReactNode;detail?:string}){return <Card><CardContent><p className="text-[11px] text-[var(--ds-text-subtle)]">{label}</p><p className="mt-1 text-xl font-semibold">{value}</p>{detail&&<p className="mt-1 text-[10px] text-[var(--ds-text-muted)]">{detail}</p>}</CardContent></Card>}
