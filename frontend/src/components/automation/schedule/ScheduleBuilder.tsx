import type {AutomationSchedule,ScheduleType} from "../../../types/schedule";import {Input,Select} from "../../ui";const days=['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];export default function ScheduleBuilder({value,onChange}:{value:AutomationSchedule;onChange(value:AutomationSchedule):void}){return <div className="grid gap-4 sm:grid-cols-2"><Select label="Schedule type" value={value.type} onChange={e=>onChange({...value,type:e.target.value as ScheduleType})}><option value="interval">Interval</option><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="once">Once</option></Select><Select label="Schedule state" value={value.enabled?'enabled':'disabled'} onChange={e=>onChange({...value,enabled:e.target.value==='enabled'})}><option value="enabled">Enabled</option><option value="disabled">Disabled</option></Select>{value.type==='interval'&&<Input label="Interval in minutes" type="number" min={1} max={10080} value={value.intervalMinutes??15} onChange={e=>onChange({...value,intervalMinutes:Number(e.target.value)})}/>} {(value.type==='daily'||value.type==='weekly')&&<Input label="Time" type="time" value={value.time??''} onChange={e=>onChange({...value,time:e.target.value})}/>} {value.type==='weekly'&&<Select label="Day" value={value.day??'Monday'} onChange={e=>onChange({...value,day:e.target.value})}>{days.map(day=><option key={day}>{day}</option>)}</Select>} {value.type==='once'&&<Input label="Run at" type="datetime-local" value={value.at??''} onChange={e=>onChange({...value,at:e.target.value})}/>}</div>}
