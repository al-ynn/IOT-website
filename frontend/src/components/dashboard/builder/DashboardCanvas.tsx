import DashboardGrid from "../layout/DashboardGrid";import {useDashboardStore} from "../../../store/dashboard.store";import {Card,EmptyState} from "../../ui";export default function DashboardCanvas({preview=false}:{preview?:boolean}){const widgets=useDashboardStore(s=>s.widgets);const updateLayout=useDashboardStore(s=>s.updateLayout);const select=useDashboardStore(s=>s.selectWidget);if(!widgets.length)return <Card><EmptyState title="Canvas is empty" description="Choose a widget from the library to begin."/></Card>;return <DashboardGrid widgets={widgets} editable={!preview} onLayoutChange={updateLayout} onSelect={preview?undefined:select}/>}
