import {useEffect,useState} from "react";import {getDevices} from "../../../services/device.service";import type {Device} from "../../../types/device";import {useDashboardStore} from "../../../store/dashboard.store";import {Card,CardContent,EmptyState,Input,SectionTitle,Select} from "../../ui";
export default function WidgetSettings(){const selected=useDashboardStore(s=>s.selectedWidget);const widget=useDashboardStore(s=>s.widgets.find(w=>w.id===selected));const update=useDashboardStore(s=>s.updateWidget);const [devices,setDevices]=useState<Device[]>([]);useEffect(()=>{getDevices().then(setDevices).catch(()=>setDevices([]))},[]);if(!widget)return <Card><EmptyState title="No widget selected" description="Select a widget on the canvas to configure it."/></Card>;const source=widget.settings.datasource;const setSettings=(data:Partial<typeof widget.settings>)=>update(widget.id,{settings:{...widget.settings,...data}});const setSource=(data:Partial<NonNullable<typeof source>>)=>setSettings({datasource:{deviceId:source?.deviceId??"",telemetryKey:source?.telemetryKey??"",...source,...data}});return <Card><CardContent className="space-y-4"><SectionTitle>Widget settings</SectionTitle><Input label="Title" value={widget.settings.title} onChange={e=>setSettings({title:e.target.value})} error={!widget.settings.title.trim()?"Title is required.":undefined}/><Select label="Device" value={source?.deviceId??""} onChange={e=>setSource({deviceId:e.target.value})}><option value="">Select a device</option>{devices.map(device=><option key={device.id} value={device.id}>{device.name}</option>)}</Select>{!["device","status"].includes(widget.type)&&<><Input label="Telemetry metric" placeholder="e.g. temperature" value={source?.telemetryKey??""} onChange={e=>setSource({telemetryKey:e.target.value})}/><Input label="Unit" placeholder="Optional" value={source?.unit??""} onChange={e=>setSource({unit:e.target.value})}/></>}{widget.type==="chart"&&<Select label="Chart style" value={widget.settings.chartType??"line"} onChange={e=>setSettings({chartType:e.target.value as "line"|"area"|"bar"})}><option value="line">Line</option><option value="area">Area</option><option value="bar">Bar</option></Select>}{widget.type==="gauge"&&<div className="grid grid-cols-2 gap-3"><Input label="Minimum" type="number" value={widget.settings.minimum??0} onChange={e=>setSettings({minimum:Number(e.target.value)})}/><Input label="Maximum" type="number" value={widget.settings.maximum??100} onChange={e=>setSettings({maximum:Number(e.target.value)})}/></div>}</CardContent></Card>}
