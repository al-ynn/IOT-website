import type {TelemetryRecord} from "../../../types/telemetry";import ChartFrame from "./ChartFrame";export default function LineChart(props:{records:TelemetryRecord[];loading?:boolean;error?:string}){return <ChartFrame {...props} type="line"/>}
