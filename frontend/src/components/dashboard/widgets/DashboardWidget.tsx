import type {ReactNode} from "react";import {AlertCircle,GripVertical,MessageSquare} from "lucide-react";import {Card,LoadingState} from "../../ui";
export default function DashboardWidget({title,children,loading=false,error,empty=false,emptyText="No data available.",editable=false,onComment}:{title:string;children?:ReactNode;loading?:boolean;error?:string;empty?:boolean;emptyText?:string;editable?:boolean;onComment?:()=>void}){return <Card className="flex h-full min-h-32 flex-col overflow-hidden" aria-label={`${title} widget`}><div className="flex h-10 shrink-0 items-center justify-between border-b border-[var(--ds-border-subtle)] px-3"><h3 className="truncate text-xs font-semibold">{title}</h3><div className="flex items-center gap-2">{onComment&&<button type="button" aria-label={`Comment on ${title} widget`} onClick={e=>{e.stopPropagation();onComment()}} className="rounded p-1 hover:bg-white/[.06]"><MessageSquare size={14}/></button>}{editable&&<GripVertical size={14} className="drag-handle cursor-grab text-[var(--ds-text-subtle)]" aria-label="Drag widget"/>}</div></div><div className="min-h-0 flex-1 p-3">{loading?<LoadingState label="Loading widget..." className="h-full min-h-20"/>:error?<div role="alert" className="flex h-full min-h-20 items-center justify-center gap-2 text-center text-xs text-[var(--ds-danger)]"><AlertCircle size={15}/>{error}</div>:empty?<div className="flex h-full min-h-20 items-center justify-center text-center text-xs text-[var(--ds-text-subtle)]">{emptyText}</div>:children}</div></Card>}
