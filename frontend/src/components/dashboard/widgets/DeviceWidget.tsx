import {Battery,MapPin} from "lucide-react";import {StatusIndicator} from "../../ui";import type {Device} from "../../../types/device";export default function DeviceWidget({device}:{device:Device}){const status=device.status==="online"?"online":device.status==="maintenance"?"warning":"offline";return <div className="space-y-3"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold">{device.name}</p><p className="text-xs text-[var(--ds-text-subtle)]">{device.type}</p></div><StatusIndicator status={status}/></div>{device.location&&<p className="flex items-center gap-2 text-xs text-[var(--ds-text-muted)]"><MapPin size={13}/>{device.location.name}</p>}{device.battery!==undefined&&<p className="flex items-center gap-2 text-xs text-[var(--ds-text-muted)]"><Battery size={13}/>{device.battery}%</p>}</div>}
