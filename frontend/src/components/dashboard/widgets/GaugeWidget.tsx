export default function GaugeWidget({value,min=0,max=100,unit}:{value:number;min?:number;max?:number;unit?:string}){const percent=Math.max(0,Math.min(100,((value-min)/(max-min||1))*100));return <div className="flex h-full items-center justify-center"><div className="relative grid h-24 w-24 place-items-center rounded-full" style={{background:`conic-gradient(var(--ds-primary) ${percent}%, var(--ds-surface-elevated) 0)`}} role="img" aria-label={`${value}${unit??""}, ${Math.round(percent)} percent of range`}><div className="grid h-[76px] w-[76px] place-items-center rounded-full bg-[var(--ds-card)]"><span className="text-lg font-semibold">{value}<span className="text-xs text-[var(--ds-text-muted)]">{unit}</span></span></div></div></div>}
