import {BodyText} from "../../ui";export default function MetricWidget({value,label,unit,trend}:{value:number|string|null;label?:string;unit?:string;trend?:string}){return <div className="flex h-full flex-col justify-center"><p className="text-2xl font-semibold tracking-tight">{value??"—"}{value!==null&&unit&&<span className="ml-1 text-sm font-medium text-[var(--ds-text-muted)]">{unit}</span>}</p>{label&&<BodyText className="mt-1 text-xs">{label}</BodyText>}{trend&&<p className="mt-2 text-[11px] text-[var(--ds-accent)]">{trend}</p>}</div>}
