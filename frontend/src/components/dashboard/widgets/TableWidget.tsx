import {DataTable} from "../../ui";import type {TelemetryRecord} from "../../../types/telemetry";export default function TableWidget({records}:{records:TelemetryRecord[]}){return <DataTable rows={records.slice(-8).reverse()} getRowKey={r=>r.id} caption="Recent telemetry" columns={[{key:"time",header:"Time",render:r=><span className="whitespace-nowrap">{new Date(r.recordedAt).toLocaleString()}</span>},{key:"metric",header:"Metric",render:r=>r.key},{key:"value",header:"Value",align:"right",render:r=><span>{r.value} {r.unit}</span>}]} empty="No telemetry records."/>}
