import type {Device} from "../../../types/device";import {Card,CardContent,CardHeader} from "../../ui";import DeviceStatus from "../status/DeviceStatus";export default function ConnectionCard({device}:{device:Device}){return <Card><CardHeader title="Connection"/><CardContent><dl className="grid grid-cols-2 gap-4 text-xs"><div><dt className="text-[var(--ds-text-subtle)]">State</dt><dd className="mt-1"><DeviceStatus status={device.status}/></dd></div><div><dt className="text-[var(--ds-text-subtle)]">Protocol</dt><dd className="mt-1 uppercase">{device.protocol||"Unavailable"}</dd></div><div><dt className="text-[var(--ds-text-subtle)]">Last seen</dt><dd className="mt-1">{device.lastSeen?new Date(device.lastSeen).toLocaleString():"Unavailable"}</dd></div><div><dt className="text-[var(--ds-text-subtle)]">MAC address</dt><dd className="mt-1">{device.macAddress??"Unavailable"}</dd></div></dl></CardContent></Card>}
