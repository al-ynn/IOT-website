import {ExternalLink} from "lucide-react";import {Link} from "react-router-dom";import type {Device} from "../../../types/device";import {Button,Card,CardContent} from "../../ui";import DeviceStatus from "../status/DeviceStatus";
export default function DeviceCard({device}:{device:Device}){return <Card><CardContent><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h2 className="truncate text-sm font-semibold">{device.name}</h2><p className="mt-1 truncate text-xs text-[var(--ds-text-muted)]">{device.type}</p></div><DeviceStatus status={device.status}/></div><dl className="mt-4 grid grid-cols-2 gap-3 text-xs"><div><dt className="text-[var(--ds-text-subtle)]">Connection</dt><dd className="mt-1 uppercase text-[var(--ds-text-muted)]">{device.protocol||"Unknown"}</dd></div><div><dt className="text-[var(--ds-text-subtle)]">Last activity</dt><dd className="mt-1 text-[var(--ds-text-muted)]">{device.lastSeen?new Date(device.lastSeen).toLocaleString():"Unavailable"}</dd></div></dl><Link to={`/app/devices/${device.id}`} className="mt-4 block"><Button size="small" variant="outline" className="w-full" leadingIcon={<ExternalLink size={14}/>}>Open device</Button></Link></CardContent></Card>}
