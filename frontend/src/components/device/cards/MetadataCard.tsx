import type {Device} from "../../../types/device";import {Card,CardContent,CardHeader} from "../../ui";export default function MetadataCard({device}:{device:Device}){const rows=[['Identifier',device.serialNumber],['Type',device.type],['Location',device.location?.name],['Firmware',device.firmwareVersion]];return <Card><CardHeader title="Device information"/><CardContent><dl className="space-y-3">{rows.map(([label,value])=><div key={label} className="flex items-start justify-between gap-3 border-b border-[var(--ds-border-subtle)] pb-2 text-xs last:border-0"><dt className="text-[var(--ds-text-subtle)]">{label}</dt><dd className="text-right text-[var(--ds-text-muted)]">{value||"Unavailable"}</dd></div>)}</dl></CardContent></Card>}
