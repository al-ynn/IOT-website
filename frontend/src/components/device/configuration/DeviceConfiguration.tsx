import type {Device} from "../../../types/device";import {Card,CardContent,CardHeader,Input} from "../../ui";export default function DeviceConfiguration({device}:{device:Device}){return <Card><CardHeader title="Configuration" description="Read-only fields supported by the current device API. Device updates are not currently available."/><CardContent className="grid gap-4 sm:grid-cols-2"><Input label="Name" value={device.name} readOnly/><Input label="Type" value={device.type} readOnly/><Input label="Location" value={device.location??""} readOnly/><Input label="Protocol" value={device.protocol} readOnly/></CardContent></Card>}
