import {Settings,Trash2} from "lucide-react";import type {Device} from "../../../types/device";import {BodyText,Button,PageTitle} from "../../ui";import DeviceStatus from "../status/DeviceStatus";
export default function DeviceHeader({device,canManage,onDelete}:{device:Device;canManage:boolean;onDelete():void}){return <header className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><div className="flex flex-wrap items-center gap-3"><PageTitle>{device.name}</PageTitle><DeviceStatus status={device.status}/></div><BodyText className="mt-1">{device.type} · {device.protocol}</BodyText></div>{canManage&&<div className="flex gap-2"><Button variant="outline" disabled title="Device updates are not supported by the current API" leadingIcon={<Settings size={14}/>}>Settings</Button><Button variant="danger" onClick={onDelete} leadingIcon={<Trash2 size={14}/>}>Delete</Button></div>}</header>}
