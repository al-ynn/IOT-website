import {Link,useSearchParams} from "react-router-dom";const tabs=[['Overview','overview'],['Telemetry','telemetry'],['Analytics','analytics'],['Configuration','configuration'],['Credentials','credentials'],['Security','security'],['Lifecycle','lifecycle'],['Activity','activity'],['Commands','commands']] as const;
export default function DeviceTabs({deviceId}:{deviceId:string}){const [params]=useSearchParams();const active=params.get('tab')??'overview';return <nav aria-label="Device sections" className="overflow-x-auto border-b border-[var(--ds-border-subtle)]"><div role="tablist" className="flex min-w-max gap-1">{tabs.map(([label,tab])=><Link key={tab} role="tab" aria-selected={active===tab} to={tab==='overview'?`/app/devices/${deviceId}`:`/app/devices/${deviceId}?tab=${tab}`} className={`border-b-2 px-3 py-2 text-xs font-medium focus-visible:ring-2 focus-visible:ring-[var(--ds-focus)] ${active===tab?"border-[var(--ds-primary)] text-[var(--ds-text)]":"border-transparent text-[var(--ds-text-muted)] hover:text-[var(--ds-text)]"}`}>{label}</Link>)}</div></nav>}
