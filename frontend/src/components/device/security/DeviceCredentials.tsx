import {Copy,KeyRound} from "lucide-react";import type {Device} from "../../../types/device";import {Button,Card,CardContent,CardHeader,EmptyState} from "../../ui";const mask=(value:string)=>value.length>8?`${value.slice(0,4)}••••${value.slice(-4)}`:"••••••••";
export default function DeviceCredentials({device,canManage}:{device:Device;canManage:boolean}){const credentials=device.credentials;if(!credentials)return <EmptyState title="Credentials unavailable" description="The device API does not expose credentials for this device." icon={<KeyRound size={18}/>}/>;return <Card><CardHeader title="Device credentials" description="Sensitive values are masked by default."/><CardContent className="space-y-3">{Object.entries(credentials).map(([label,value])=><div key={label} className="flex items-center justify-between gap-3 rounded-[8px] border border-[var(--ds-border-subtle)] p-3"><div><p className="text-[10px] uppercase text-[var(--ds-text-subtle)]">{label}</p><code className="text-xs">{mask(value)}</code></div>{canManage&&<Button size="compact" variant="ghost" aria-label={`Copy ${label}`} onClick={()=>navigator.clipboard.writeText(value)}><Copy size={14}/></Button>}</div>)}</CardContent></Card>}
