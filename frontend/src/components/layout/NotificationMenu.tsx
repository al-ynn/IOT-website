import {useEffect,useRef,useState} from "react";import {Bell,History} from "lucide-react";import {Link} from "react-router-dom";import NotificationUnavailable from "../notifications/states/NotificationUnavailable";
export default function NotificationMenu(){const [open,setOpen]=useState(false);const root=useRef<HTMLDivElement>(null);useEffect(()=>{const close=(e:KeyboardEvent)=>{if(e.key==="Escape")setOpen(false)};document.addEventListener("keydown",close);return()=>document.removeEventListener("keydown",close)},[]);return <div ref={root} className="relative"><button type="button" aria-label="Notifications" aria-expanded={open} aria-haspopup="dialog" onClick={()=>setOpen(v=>!v)} className="grid h-9 w-9 place-items-center rounded-[8px] text-[var(--ds-text-muted)] hover:bg-white/[.05] hover:text-[var(--ds-text)] focus-visible:ring-2 focus-visible:ring-[var(--ds-focus)]"><Bell size={17}/></button>{open&&<div role="dialog" aria-label="Notifications" className="absolute right-0 top-11 z-50 w-[min(22rem,calc(100vw-2rem))] rounded-[10px] border border-[var(--ds-border)] bg-[var(--ds-surface-elevated)] shadow-2xl"><div className="flex items-center justify-between border-b border-[var(--ds-border-subtle)] px-4 py-3"><span className="text-sm font-semibold">Notifications</span><Link to="/app/activity" onClick={()=>setOpen(false)} className="flex items-center gap-1 text-xs text-[var(--ds-primary)]"><History size={13}/>Activity</Link></div><NotificationUnavailable compact/><Link to="/app/notifications" onClick={()=>setOpen(false)} className="block border-t border-[var(--ds-border-subtle)] px-4 py-3 text-center text-xs font-medium text-[var(--ds-primary)] focus-visible:ring-2 focus-visible:ring-[var(--ds-focus)]">View notification center</Link></div>}</div>}
