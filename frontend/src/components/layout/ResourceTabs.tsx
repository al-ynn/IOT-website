import {Link} from "react-router-dom";
export interface ResourceTabItem{id:string;label:string;to:string;available?:boolean}
export default function ResourceTabs({items,activeId,label}:{items:ResourceTabItem[];activeId:string;label:string}){const availableItems=items.filter(item=>item.available!==false);return <nav aria-label={label} className="overflow-x-auto overscroll-x-contain border-b border-[var(--ds-border-subtle)]"><div className="flex min-w-max gap-1">{availableItems.map(item=><Link key={item.id} aria-current={activeId===item.id?"page":undefined} to={item.to} className={`min-h-10 border-b-2 px-3 py-2 text-xs font-medium focus-visible:ring-2 focus-visible:ring-[var(--ds-focus)] ${activeId===item.id?"border-[var(--ds-primary)] bg-[var(--ds-surface-muted)] text-[var(--ds-text)]":"border-transparent text-[var(--ds-text-muted)] hover:text-[var(--ds-text)]"}`}>{item.label}</Link>)}</div></nav>}
