import {useEffect,useRef,useState} from "react";import {Search,X} from "lucide-react";import {EmptyState,Input} from "../ui";
export default function SearchCommand(){const [open,setOpen]=useState(false);const [query,setQuery]=useState("");const input=useRef<HTMLInputElement>(null);useEffect(()=>{const key=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setOpen(true)}if(e.key==="Escape")setOpen(false)};document.addEventListener("keydown",key);return()=>document.removeEventListener("keydown",key)},[]);useEffect(()=>{if(open)setTimeout(()=>input.current?.focus(),0)},[open]);return <><button type="button" onClick={()=>setOpen(true)} aria-label="Open global search" className="hidden h-9 items-center gap-2 rounded-[8px] border border-[var(--ds-border-subtle)] bg-[var(--ds-surface)] px-3 text-xs text-[var(--ds-text-subtle)] hover:border-[var(--ds-border)] sm:flex"><Search size={14}/>Search<span className="ml-5 rounded border border-[var(--ds-border)] px-1.5 py-0.5 text-[10px]">Ctrl K</span></button>{open&&<div className="fixed inset-0 z-[70] flex items-start justify-center bg-[var(--ds-overlay)] px-4 pt-[12vh]" role="dialog" aria-modal="true" aria-label="Global search"><button type="button" aria-label="Close search" className="absolute inset-0" onClick={()=>setOpen(false)}/><div className="relative w-full max-w-xl rounded-[10px] border border-[var(--ds-border)] bg-[var(--ds-surface-elevated)] p-3 shadow-2xl"><button type="button" onClick={()=>setOpen(false)} aria-label="Close search" className="absolute right-4 top-4 z-10 text-[var(--ds-text-subtle)]"><X size={16}/></button><Input ref={input} aria-label="Search workspace" placeholder="Search the workspace" leadingIcon={<Search size={15}/>} value={query} onChange={e=>setQuery(e.target.value)} className="pr-8"/><EmptyState title={query?"No results":"Search workspace"} description={query?"Global search is not connected to a search service yet.":"Enter a term to search when a search provider is available."} icon={<Search size={18}/>} className="min-h-36"/></div></div>}</>}
