import type {LucideIcon} from "lucide-react";
import {Link} from "react-router-dom";
import {ArrowRight} from "lucide-react";
import {Badge,BodyText,Button,Card,CardContent,PageTitle,SectionTitle} from "../ui";

export function PublicHero({eyebrow,title,description,primary={label:"Get started",to:"/login"},secondary={label:"Explore features",to:"/features"}}:{eyebrow:string;title:string;description:string;primary?:{label:string;to:string};secondary?:{label:string;to:string}}){return <section className="border-b border-[var(--ds-border-subtle)]"><div className="mx-auto max-w-7xl px-4 py-16 sm:px-6 sm:py-20"><Badge tone="info">{eyebrow}</Badge><PageTitle className="mt-4 max-w-3xl text-3xl sm:text-4xl">{title}</PageTitle><BodyText className="mt-4 max-w-2xl text-base leading-7">{description}</BodyText><div className="mt-6 flex flex-wrap gap-3"><Link to={primary.to}><Button>{primary.label}<ArrowRight size={15}/></Button></Link><Link to={secondary.to}><Button variant="outline">{secondary.label}</Button></Link></div></div></section>}
export function PublicSection({title,description,children}:{title:string;description?:string;children:React.ReactNode}){return <section className="border-b border-[var(--ds-border-subtle)]"><div className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><SectionTitle>{title}</SectionTitle>{description&&<BodyText className="mt-2 max-w-2xl">{description}</BodyText>}<div className="mt-6">{children}</div></div></section>}
export function FeatureCards({items}:{items:{title:string;description:string;icon?:LucideIcon;badge?:string}[]}){return <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map(item=>{const Icon=item.icon;return <Card key={item.title} variant="interactive"><CardContent>{Icon&&<Icon size={19} className="text-[var(--ds-primary)]" aria-hidden/>}<div className="mt-3 flex items-center gap-2"><h3 className="text-sm font-semibold">{item.title}</h3>{item.badge&&<Badge>{item.badge}</Badge>}</div><BodyText className="mt-2">{item.description}</BodyText></CardContent></Card>})}</div>}
