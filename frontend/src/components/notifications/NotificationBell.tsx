/* eslint-disable react-hooks/set-state-in-effect */
import {useCallback,useEffect,useRef,useState} from "react";import {Bell,CheckCheck} from "lucide-react";import {Link,useNavigate} from "react-router-dom";import {Button,Drawer,EmptyState,ErrorState,Skeleton} from "../ui";import {markNotificationRead,recentNotifications,unreadNotificationCount} from "../../services/notification.service";import type {AppNotification} from "../../types/notification";
const ago=(value:string)=>{const seconds=Math.max(1,Math.floor((Date.now()-new Date(value).getTime())/1000));if(seconds<60)return `${seconds}s ago`;if(seconds<3600)return `${Math.floor(seconds/60)}m ago`;if(seconds<86400)return `${Math.floor(seconds/3600)}h ago`;return `${Math.floor(seconds/86400)}d ago`;};
export default function NotificationBell(){const navigate=useNavigate();const button=useRef<HTMLButtonElement>(null);const [open,setOpen]=useState(false);const [count,setCount]=useState(0);const [items,setItems]=useState<AppNotification[]>([]);const [loading,setLoading]=useState(false);const [error,setError]=useState("");const refresh=useCallback(async(includeItems=false)=>{try{setCount(await unreadNotificationCount());if(includeItems)setItems(await recentNotifications());setError("");}catch{if(includeItems)setError("Notifications could not be loaded.");}finally{setLoading(false);}},[]);useEffect(()=>{void refresh();const timer=window.setInterval(()=>void refresh(),60000);const focus=()=>void refresh();window.addEventListener("focus",focus);return()=>{window.clearInterval(timer);window.removeEventListener("focus",focus);};},[refresh]);const show=()=>{setOpen(true);setLoading(true);void refresh(true);};const close=()=>{setOpen(false);window.setTimeout(()=>button.current?.focus(),0);};const read=async(item:AppNotification)=>{if(!item.isRead){await markNotificationRead(item.id);setCount(v=>Math.max(0,v-1));setItems(v=>v.map(n=>n.id===item.id?{...n,isRead:true}:n));}};const view=async(item:AppNotification)=>{await read(item);close();if(item.deepLink)navigate(item.deepLink);};return <><button ref={button} type="button" aria-label={`Notifications${count?`, ${count} unread`:""}`} onClick={show} className="relative grid h-9 w-9 place-items-center rounded-[8px] text-[var(--ds-text-muted)] hover:bg-white/[.05] hover:text-[var(--ds-text)]"><Bell size={17}/>{count>0&&<span className="absolute right-0.5 top-0.5 min-w-4 rounded-full bg-[var(--ds-primary-hover)] px-1 text-center text-[9px] font-semibold text-white" aria-hidden="true">{count>99?"99+":count}</span>}</button><Drawer open={open} onClose={close} title="Notifications" description={`${count} unread`} footer={<Link to="/app/notifications" onClick={close} className="text-xs font-medium text-[var(--ds-primary)]">View all notifications</Link>}>{loading?<Skeleton className="h-48"/>:error?<ErrorState description={error} retry={()=>void refresh(true)}/>:items.length===0?<EmptyState title="No notifications"/>:<ul className="space-y-2">{items.map(item=><li key={item.id} className={`rounded-lg border p-3 ${item.isRead?"border-[var(--ds-border-subtle)]":"border-[var(--ds-primary)]"}`}><div className="flex gap-2"><div className="min-w-0 flex-1"><p className="break-words text-xs font-semibold">{item.title}{!item.isRead&&<span className="ml-2 text-[10px] text-[var(--ds-primary)]">Unread</span>}</p><p className="mt-1 break-words text-xs text-[var(--ds-text-muted)]">{item.body}</p><time title={new Date(item.createdAt).toLocaleString()} className="mt-1 block text-[10px] text-[var(--ds-text-subtle)]">{ago(item.createdAt)}</time></div>{!item.isRead&&<button aria-label={`Mark ${item.title} read`} onClick={()=>void read(item)}><CheckCheck size={15}/></button>}</div>{item.deepLink&&<Button className="mt-2" size="compact" variant="outline" onClick={()=>void view(item)}>View</Button>}</li>)}</ul>}</Drawer></>}
