import {Input,Select} from "../../ui";
export interface NotificationFilterValue{category:string;readState:string;date:string}
export default function NotificationFilters({value,onChange,disabled=false}:{value:NotificationFilterValue;onChange(value:NotificationFilterValue):void;disabled?:boolean}){return <div className="grid gap-3 sm:grid-cols-3" aria-label="Notification filters"><Select aria-label="Category" disabled={disabled} value={value.category} onChange={event=>onChange({...value,category:event.target.value})}><option value="all">All categories</option><option value="device">Device</option><option value="automation">Automation</option><option value="organization">Organization</option></Select><Select aria-label="Read state" disabled={disabled} value={value.readState} onChange={event=>onChange({...value,readState:event.target.value})}><option value="all">Read and unread</option><option value="unread">Unread</option><option value="read">Read</option></Select><Input aria-label="Date" type="date" disabled={disabled} value={value.date} onChange={event=>onChange({...value,date:event.target.value})}/></div>}
