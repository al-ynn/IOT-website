import { Badge } from "../../ui";import type { Notification } from "../../../types/notification";
export default function NotificationItem({notification,onRead}:{notification:Notification;onRead?(id:string):void}){return <li className="rounded-lg border border-[var(--ds-border-subtle)] p-4"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><h3 className="text-sm font-semibold text-[var(--ds-text)]">{notification.title}</h3>{!notification.read&&<span className="h-2 w-2 rounded-full bg-[var(--ds-primary)]" role="status" aria-label="Unread"/>}</div><p className="mt-1 text-sm text-[var(--ds-text-muted)]">{notification.message}</p></div><Badge>{notification.severity}</Badge></div><div className="mt-3 flex items-center justify-between text-xs text-[var(--ds-text-subtle)]"><time dateTime={notification.createdAt}>{new Date(notification.createdAt).toLocaleString()}</time>{!notification.read&&onRead&&<button className="text-[var(--ds-primary)] focus-visible:ring-2 focus-visible:ring-[var(--ds-focus)]" onClick={()=>onRead(notification.id)}>Mark read</button>}</div></li>}
