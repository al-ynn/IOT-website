import{Link}from"react-router-dom";
import type {AppNotification} from "../../../types/notification";
export default function NotificationItem({notification,onRead}:{notification:AppNotification;onRead?(id:string):void}){return <li className="rounded-lg border border-[var(--ds-border-subtle)] p-4"><div><h3 className="text-sm font-semibold text-[var(--ds-text)]">{notification.title}</h3>{!notification.isRead&&<span className="text-xs text-[var(--ds-primary)]">Unread</span>}<p className="mt-1 text-sm text-[var(--ds-text-muted)]">{notification.body}</p>{notification.requiresAction&&<p className="mt-1 text-xs font-medium">Action required</p>}{notification.actions.length>0&&<div className="mt-2 flex flex-wrap gap-2">{notification.actions.map(action=><Link key={action.key} to={action.href} className="rounded border px-2 py-1 text-xs">{action.label}</Link>)}</div>}</div><div className="mt-3 flex items-center justify-between text-xs text-[var(--ds-text-subtle)]"><time dateTime={notification.createdAt}>{new Date(notification.createdAt).toLocaleString()}</time>{!notification.isRead&&onRead&&<button onClick={()=>onRead(notification.id)}>Mark read</button>}</div></li>}
