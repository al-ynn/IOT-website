import {NavLink} from "react-router-dom";const tabs=[['Overview','/app/organization'],['Members','/app/members'],['Invitations','/app/invitations'],['Roles','/app/roles'],['Settings','/app/settings/organization']] as const;export default function OrganizationTabs(){return <nav aria-label="Organization sections" className="overflow-x-auto border-b border-[var(--ds-border-subtle)]"><div className="flex min-w-max">{tabs.map(([label,to])=><NavLink key={to} to={to} end className={({isActive})=>`border-b-2 px-3 py-2 text-xs font-medium ${isActive?'border-[var(--ds-primary)] text-[var(--ds-text)]':'border-transparent text-[var(--ds-text-muted)] hover:text-[var(--ds-text)]'}`}>{label}</NavLink>)}</div></nav>}
