import type {OrganizationMember} from "../../../types/invitation";import {Badge,Card,CardContent} from "../../ui";export default function MemberCard({member}:{member:OrganizationMember}){return <Card><CardContent><div className="flex justify-between gap-3"><div><p className="text-sm font-medium">{member.name}</p><p className="mt-1 text-xs text-[var(--ds-text-muted)]">{member.email}</p></div><Badge>{member.role}</Badge></div><p className="mt-3 text-[11px] text-[var(--ds-text-subtle)]">Joined {new Date(member.joinedAt).toLocaleDateString()}</p></CardContent></Card>}
