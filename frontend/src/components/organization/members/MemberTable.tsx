import type {OrganizationMember} from "../../../types/invitation";import {Badge,DataTable} from "../../ui";export default function MemberTable({members}:{members:OrganizationMember[]}){return <DataTable rows={members} getRowKey={m=>m.id} caption="Organization members" empty="No members match this filter." columns={[{key:'member',header:'Member',render:m=><div><p className="font-medium text-[var(--ds-text)]">{m.name}</p><p className="text-[11px] text-[var(--ds-text-subtle)]">{m.email}</p></div>},{key:'role',header:'Role',render:m=><Badge>{m.role}</Badge>},{key:'status',header:'Status',render:()=><span className="text-[11px] text-[var(--ds-text-subtle)]">Unavailable</span>},{key:'joined',header:'Joined',render:m=>new Date(m.joinedAt).toLocaleDateString()},{key:'actions',header:'Actions',align:'right',render:()=><span className="text-[11px] text-[var(--ds-text-subtle)]">Read only</span>}]} />}
