import {Check} from "lucide-react";import type {Permission} from "../../../types/permission";
export default function PermissionList({permissions}:{permissions:Permission[]}){const groups=permissions.reduce<Record<string,Permission[]>>((result,permission)=>{const category=permission.category||'other';(result[category]??=[]).push(permission);return result},{});return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{Object.entries(groups).map(([category,items])=><section key={category} className="rounded-[8px] border border-[var(--ds-border-subtle)] p-3"><h3 className="text-xs font-semibold capitalize">{category}</h3><ul className="mt-2 space-y-2">{items.map(permission=><li key={permission.id} className="flex items-center gap-2 text-xs text-[var(--ds-text-muted)]"><Check size={13} className="text-[var(--ds-success)]"/>{permission.name}</li>)}</ul></section>)}</div>}
