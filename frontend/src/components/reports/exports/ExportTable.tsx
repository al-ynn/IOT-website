import {Badge,EmptyState} from "../../ui";import type {ExportFile} from "../../../types/export";
export default function ExportTable({files}:{files:ExportFile[]}){if(!files.length)return <EmptyState title="No exports" description="No backend-generated export files are available."/>;return <div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><caption className="sr-only">Export history</caption><thead className="border-b border-[var(--ds-border)] text-xs text-[var(--ds-text-muted)]"><tr><th className="p-3">File</th><th className="p-3">Type</th><th className="p-3">Created</th><th className="p-3">Status</th><th className="p-3">Size</th><th className="p-3">Download</th></tr></thead><tbody>{files.map(file=><tr key={file.id} className="border-b border-[var(--ds-border-subtle)]"><td className="p-3 font-medium">{file.name}</td><td className="p-3 uppercase">{file.format}</td><td className="p-3"><time dateTime={file.createdAt}>{new Date(file.createdAt).toLocaleString()}</time></td><td className="p-3"><Badge tone="success">Ready</Badge></td><td className="p-3 text-[var(--ds-text-muted)]">Unavailable</td><td className="p-3"><a href={file.url} className="text-[var(--ds-primary)] hover:underline" aria-label={`Download ${file.name}`}>Download</a></td></tr>)}</tbody></table></div>}
