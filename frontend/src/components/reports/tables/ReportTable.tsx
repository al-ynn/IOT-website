import {Badge} from "../../ui";import type {Report} from "../../../types/report";import ReportEmpty from "../states/ReportEmpty";
export default function ReportTable({reports}:{reports:Report[]}){if(!reports.length)return <ReportEmpty/>;return <div className="overflow-x-auto"><table className="w-full min-w-[640px] text-left text-sm"><caption className="sr-only">Generated reports</caption><thead className="border-b border-[var(--ds-border)] text-xs text-[var(--ds-text-muted)]"><tr><th className="p-3">Name</th><th className="p-3">Type</th><th className="p-3">Created</th><th className="p-3">Status</th><th className="p-3">Owner</th></tr></thead><tbody>{reports.map(report=><tr key={report.id} className="border-b border-[var(--ds-border-subtle)]"><td className="p-3 font-medium">{report.name}</td><td className="p-3">{report.reportType.replaceAll("_"," ")}</td><td className="p-3"><time dateTime={report.createdAt}>{new Date(report.createdAt).toLocaleString()}</time></td><td className="p-3"><Badge>{report.lastRun?.status??"Never"}</Badge></td><td className="p-3 text-[var(--ds-text-muted)]">{report.createdBy?.name??"Unavailable"}</td></tr>)}</tbody></table></div>}
