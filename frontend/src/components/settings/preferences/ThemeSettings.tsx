import { Moon, Sun } from "lucide-react";
import { Card, CardContent, CardHeader } from "../../ui";
import { useTheme, type Theme } from "../../../design-system/themes/ThemeProvider";
import { cn } from "../../../utils/cn";

export default function ThemeSettings(){const {theme,setTheme}=useTheme();const choices:{value:Theme;label:string;description:string;icon:typeof Moon}[]=[{value:"dark",label:"Dark",description:"Low-glare workspace for monitoring.",icon:Moon},{value:"light",label:"Light",description:"Bright workspace with stronger contrast.",icon:Sun}];return <Card><CardHeader title="Appearance" description="Your selection is saved on this browser."/><CardContent><fieldset><legend className="sr-only">Color theme</legend><div className="grid gap-3 sm:grid-cols-2">{choices.map(({value,label,description,icon:Icon})=><button key={value} type="button" aria-pressed={theme===value} onClick={()=>setTheme(value)} className={cn("rounded-lg border p-4 text-left transition focus-visible:ring-2 focus-visible:ring-[var(--ds-focus)]",theme===value?"border-[var(--ds-primary)] bg-[var(--ds-primary-soft)]":"border-[var(--ds-border)] hover:bg-white/[.04]")}><Icon size={18} className="text-[var(--ds-primary)]"/><span className="mt-3 block text-sm font-semibold text-[var(--ds-text)]">{label}</span><span className="mt-1 block text-xs text-[var(--ds-text-muted)]">{description}</span></button>)}</div></fieldset></CardContent></Card>}
