import {Input,LoadingState,Select} from "../../ui";import type {Device} from "../../../types/device";export default function DeviceSelector({devices,value,onChange,query,onQueryChange,loading=false,error}:{devices:Device[];value:string;onChange(value:string):void;query:string;onQueryChange(value:string):void;loading?:boolean;error?:string}){if(loading)return <LoadingState label="Loading devices..." className="min-h-16"/>;return <div className="grid gap-2"><Input label="Find device" placeholder="Search by name or identifier" value={query} onChange={e=>onQueryChange(e.target.value)} error={error}/><Select label="Device" value={value} onChange={e=>onChange(e.target.value)}><option value="">{devices.length?"Select a device":"No devices available"}</option>{devices.map(device=><option key={device.id} value={device.id}>{device.name} · {device.serialNumber}</option>)}</Select></div>}
