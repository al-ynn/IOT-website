import {Input,Select} from "../../ui";import type {AnalyticsMetricSummary} from "../../../types/analytics";export default function MetricSelector({metrics,value,onChange,query,onQueryChange}:{metrics:AnalyticsMetricSummary[];value:string;onChange(value:string):void;query:string;onQueryChange(value:string):void}){return <div className="grid gap-2"><Input label="Find metric" placeholder="Search metrics" value={query} onChange={e=>onQueryChange(e.target.value)}/><Select label="Metric" value={value} onChange={e=>onChange(e.target.value)}><option value="">{metrics.length?"Select a metric":"No metrics reported"}</option>{metrics.map(metric=><option key={metric.metric} value={metric.metric}>{metric.metric}{metric.unit?` (${metric.unit})`:''}</option>)}</Select></div>}
