import type {AnalyticsRange} from "../../../types/analytics";import {Input,Select} from "../../ui";export default function TimeRangeSelector({range,onChange,from,to,onDatesChange}:{range:AnalyticsRange;onChange(value:AnalyticsRange):void;from:string;to:string;onDatesChange(value:{from:string;to:string}):void}){return <div className="grid gap-2"><Select label="Time range" value={range} onChange={e=>onChange(e.target.value as AnalyticsRange)}><option value="1h">Last hour</option><option value="6h">Last 6 hours</option><option value="12h">Last 12 hours</option><option value="24h">Last 24 hours</option><option value="7d">Last 7 days</option><option value="30d">Last 30 days</option><option value="custom">Custom range</option></Select>{range==='custom'&&<div className="grid grid-cols-2 gap-2"><Input label="From" type="datetime-local" value={from} onChange={e=>onDatesChange({from:e.target.value,to})}/><Input label="To" type="datetime-local" value={to} onChange={e=>onDatesChange({from,to:e.target.value})}/></div>}</div>}
