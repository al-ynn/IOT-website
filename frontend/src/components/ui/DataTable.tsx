import type {Key,ReactNode} from "react";import {cn} from "../../utils/cn";
export interface DataTableColumn<T>{key:string;header:ReactNode;render(row:T):ReactNode;align?:"left"|"center"|"right";width?:string;}
export interface DataTableProps<T>{rows:T[];columns:DataTableColumn<T>[];getRowKey(row:T):Key;caption?:string;empty?:ReactNode;compact?:boolean;className?:string;}
export function DataTable<T>({rows,columns,getRowKey,caption,empty="No records available.",compact=true,className}:DataTableProps<T>){return <div className={cn("overflow-x-auto rounded-[10px] border border-[var(--ds-border-subtle)]",className)}><table className="w-full border-collapse text-left text-sm">{caption&&<caption className="sr-only">{caption}</caption>}<thead className="bg-[var(--ds-surface-elevated)] text-[11px] font-semibold uppercase tracking-wider text-[var(--ds-text-subtle)]"><tr>{columns.map(column=><th key={column.key} scope="col" style={{width:column.width}} className={cn("whitespace-nowrap border-b border-[var(--ds-border-subtle)] px-3",compact?"h-9":"h-11",column.align==="center"&&"text-center",column.align==="right"&&"text-right")}>{column.header}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={getRowKey(row)} className="border-b border-[var(--ds-border-subtle)] last:border-0 hover:bg-white/[.025]">{columns.map(column=><td key={column.key} className={cn("px-3 text-[var(--ds-text-muted)]",compact?"h-10":"h-12",column.align==="center"&&"text-center",column.align==="right"&&"text-right")}>{column.render(row)}</td>)}</tr>)}{rows.length===0&&<tr><td colSpan={columns.length} className="h-28 text-center text-sm text-[var(--ds-text-subtle)]">{empty}</td></tr>}</tbody></table></div>;}
