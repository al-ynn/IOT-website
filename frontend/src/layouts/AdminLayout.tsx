import {useState,type ReactNode} from "react";import AppHeader from "../components/layout/AppHeader";import AppSidebar from "../components/layout/AppSidebar";import MobileSidebar from "../components/layout/MobileSidebar";import {adminNavigation} from "../navigation/admin-navigation";
export default function AdminLayout({children}:{children:ReactNode}){const [mobileOpen,setMobileOpen]=useState(false);const [collapsed,setCollapsed]=useState(false);const sidebar=<AppSidebar items={adminNavigation} collapsed={collapsed} onToggle={()=>setCollapsed(value=>!value)} onNavigate={()=>setMobileOpen(false)} admin/>;return <div className="min-h-screen bg-[var(--ds-bg)] text-[var(--ds-text)]"><div className={`fixed inset-y-0 left-0 z-40 hidden border-r border-[var(--ds-border-subtle)] transition-[width] duration-200 lg:block ${collapsed?"w-16":"w-56"}`}>{sidebar}</div><MobileSidebar open={mobileOpen} onClose={()=>setMobileOpen(false)}><AppSidebar items={adminNavigation} onNavigate={()=>setMobileOpen(false)} admin/></MobileSidebar><div className={`transition-[padding] duration-200 ${collapsed?"lg:pl-16":"lg:pl-56"}`}><AppHeader onOpenNavigation={()=>setMobileOpen(true)} context="System-wide operations" admin/><main id="main-content" className="mx-auto max-w-[1600px] p-4 sm:p-5 lg:p-6">{children}</main></div></div>}
