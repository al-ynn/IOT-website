import{useCallback}from"react";import{useParams}from"react-router-dom";import DashboardWorkspace from"../../components/dashboard/DashboardWorkspace";import{getDashboard,updateDashboard}from"../../services/dashboard.service";import{EmptyState}from"../../components/ui";import type{Dashboard}from"../../types/dashboard";
export default function DashboardDetail(){const{id}=useParams();const load=useCallback(()=>getDashboard(id!),[id]);const save=useCallback((dashboard:Dashboard)=>updateDashboard(id!,dashboard),[id]);if(!id)return <EmptyState title="Dashboard unavailable"/>;return <DashboardWorkspace load={load} save={save} resourceId={id}/>;}