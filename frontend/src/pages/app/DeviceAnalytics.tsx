import {useEffect,useState} from "react";import {useParams} from "react-router-dom";import {getDeviceMetrics} from "../../services/analytics.service";import type {DeviceAnalytics as DeviceAnalyticsData} from "../../types/analytics";import AnalyticsHeader from "../../components/analytics/layout/AnalyticsHeader";import KpiCard from "../../components/analytics/cards/KpiCard";import TelemetryPanel from "../../components/telemetry/layout/TelemetryPanel";import {Badge,DataTable,EmptyState,ErrorState,LoadingState} from "../../components/ui";
export default function DeviceAnalytics(){const {id}=useParams();const [data,setData]=useState<DeviceAnalyticsData|null>(null);const [error,setError]=useState('');useEffect(()=>{if(id)getDeviceMetrics(id).then(setData).catch(()=>setError('Unable to load device analytics.'))},[id]);if(error)return <ErrorState description={error}/>;if(!data||!id)return <LoadingState label="Loading device analytics..."/>;return <div className="space-y-5"><AnalyticsHeader title={data.device.name} description="Device performance, metrics, and historical telemetry analysis."/><section className="grid gap-3 sm:grid-cols-3"><KpiCard label="Telemetry records" value={data.telemetryRecords}/><KpiCard label="Reported metrics" value={data.metrics.length}/><KpiCard label="Device state" value={<Badge tone={data.device.status==='online'?'success':'neutral'}>{data.device.status||'unknown'}</Badge>}/></section>{data.metrics.length?<DataTable rows={data.metrics} getRowKey={m=>m.metric} caption="Device metric statistics" columns={[{key:'metric',header:'Metric',render:m=>m.metric},{key:'latest',header:'Latest',align:'right',render:m=>m.latest??'—'},{key:'average',header:'Average',align:'right',render:m=>m.average},{key:'minimum',header:'Minimum',align:'right',render:m=>m.minimum},{key:'maximum',header:'Maximum',align:'right',render:m=>m.maximum},{key:'count',header:'Count',align:'right',render:m=>m.count}]}/>:<EmptyState title="No device analytics" description="This device has not reported telemetry in the selected default range."/>}<section><h2 className="mb-3 text-sm font-semibold">Metric trends</h2><TelemetryPanel fixedDeviceId={id}/></section></div>}
