import{useCallback}from"react";import{useParams}from"react-router-dom";import DashboardWorkspace from"../../components/dashboard/DashboardWorkspace";import{getPublishedDashboard}from"../../services/dashboard-publication.service";import{EmptyState}from"../../components/ui";
export default function PublishedDashboardDetail(){const{id}=useParams();const load=useCallback(()=>getPublishedDashboard(id!),[id]);if(!id)return <EmptyState title="Published Dashboard unavailable"/>;return <DashboardWorkspace load={load} save={async()=>{throw new Error("Published Dashboards are read-only")}} canEdit={false}/>}