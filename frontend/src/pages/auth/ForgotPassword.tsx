import {useState} from "react";import {Link} from "react-router-dom";import AuthLayout from "../../components/auth/AuthLayout";import {BodyText,Button,Input,PageTitle} from "../../components/ui";
export default function ForgotPassword(){const [email,setEmail]=useState("");const [notice,setNotice]=useState("");return <AuthLayout><PageTitle className="text-2xl">Reset your password</PageTitle><BodyText className="mt-2">Enter your account email to begin recovery.</BodyText><form className="mt-6 space-y-4" onSubmit={e=>{e.preventDefault();setNotice("Password recovery is not available from this deployment yet. Contact your organization administrator.")}}><Input label="Email" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/>{notice&&<p role="status" className="text-xs text-[var(--ds-warning)]">{notice}</p>}<Button type="submit" className="w-full">Continue</Button></form><Link to="/login" className="mt-5 block text-center text-xs text-[var(--ds-primary)] hover:underline">Return to login</Link></AuthLayout>}
