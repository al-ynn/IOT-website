import {Link,useSearchParams} from "react-router-dom";import AuthLayout from "../../components/auth/AuthLayout";import {Badge,BodyText,Button,PageTitle} from "../../components/ui";
export default function VerifyEmail(){const [params]=useSearchParams();const state=params.get("status");const view=state==="verified"?{tone:"success" as const,label:"Verified",title:"Email verified",text:"Your email address has been verified."}:state==="expired"?{tone:"danger" as const,label:"Expired",title:"Verification link expired",text:"This verification link is no longer valid."}:{tone:"warning" as const,label:"Pending",title:"Verify your email",text:"Email verification is pending. Follow the link sent by the authentication service."};return <AuthLayout><Badge tone={view.tone}>{view.label}</Badge><PageTitle className="mt-4 text-2xl">{view.title}</PageTitle><BodyText className="mt-3">{view.text}</BodyText><Link to="/login" className="mt-6 block"><Button variant="outline" className="w-full">Return to login</Button></Link></AuthLayout>}
