import {useEffect,useState} from "react";import {Link,useParams} from "react-router-dom";import {Eye,Settings,Share2} from "lucide-react";import {getDashboard} from "../../services/dashboard.service";import type {Dashboard} from "../../types/dashboard";import DashboardGrid from "../../components/dashboard/layout/DashboardGrid";import {BodyText,Button,EmptyState,ErrorState,LoadingState,PageTitle} from "../../components/ui";import usePermission from "../../hooks/usePermission";
export default function DashboardDetail(){const {id}=useParams();const [dashboard,setDashboard]=useState<Dashboard|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const {can}=usePermission();async function load(){if(!id)return;setLoading(true);setError("");try{setDashboard(await getDashboard(id))}catch{setError("Dashboard not found or unavailable.")}finally{setLoading(false)}}useEffect(()=>{void load()},[id]);if(loading)return <LoadingState label="Loading dashboard..."/>;if(error)return <ErrorState description={error} retry={load}/>;if(!dashboard)return null;return <div className="space-y-5"><header className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><PageTitle>{dashboard.name}</PageTitle><BodyText className="mt-1">{dashboard.description??"Operational dashboard"}</BodyText></div><div className="flex flex-wrap gap-2"><Link to={`/app/dashboard/preview?id=${dashboard.id}`}><Button variant="outline" leadingIcon={<Eye size={14}/>}>Preview</Button></Link><Button variant="outline" leadingIcon={<Share2 size={14}/>} onClick={()=>navigator.clipboard.writeText(window.location.href)}>Copy link</Button>{can("dashboard.update")&&<Link to={`/app/dashboard-builder?id=${dashboard.id}`}><Button leadingIcon={<Settings size={14}/>}>Edit</Button></Link>}</div></header>{dashboard.widgets.length?<DashboardGrid widgets={dashboard.widgets}/>:<EmptyState title="No widgets configured" description="This dashboard does not contain any widgets yet." action={can("dashboard.update")?<Link to={`/app/dashboard-builder?id=${dashboard.id}`}><Button size="small">Configure dashboard</Button></Link>:undefined}/>}</div>}
