import {LayoutTemplate} from "lucide-react";import {dashboardTemplates} from "../../data/dashboardTemplates";import {BodyText,Card,CardContent,EmptyState,PageTitle,Badge} from "../../components/ui";
export default function DashboardTemplates(){return <div className="space-y-5"><div><PageTitle>Dashboard templates</PageTitle><BodyText className="mt-1">Available starting points included with this deployment.</BodyText></div>{dashboardTemplates.length?<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{dashboardTemplates.map(template=><Card key={template.id} variant="interactive"><CardContent><LayoutTemplate size={18} className="text-[var(--ds-primary)]"/><h2 className="mt-3 text-sm font-semibold">{template.name}</h2><BodyText className="mt-1 text-xs">{template.description}</BodyText><div className="mt-3 flex items-center justify-between"><Badge>{template.category}</Badge><span className="text-[11px] text-[var(--ds-text-subtle)]">{template.widgets.length} widgets</span></div></CardContent></Card>)}</div>:<EmptyState title="No templates available" description="No dashboard template source is configured."/>}</div>}
