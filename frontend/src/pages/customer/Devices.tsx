import {useEffect,useMemo,useState} from "react";import {Link} from "react-router-dom";import {ExternalLink,Plus} from "lucide-react";import {getDevices} from "../../services/device.service";import type {Device} from "../../types/device";import {useBilling} from "../../hooks/useBilling";import usePermission from "../../hooks/usePermission";import DeviceCard from "../../components/device/cards/DeviceCard";import DeviceStatus from "../../components/device/status/DeviceStatus";import {BodyText,Button,DataTable,EmptyState,ErrorState,Input,LoadingState,PageTitle,Select} from "../../components/ui";
export default function Devices(){const [devices,setDevices]=useState<Device[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const [query,setQuery]=useState("");const [status,setStatus]=useState("all");const {checkDeviceLimit}=useBilling();const {can}=usePermission();async function load(){setLoading(true);setError("");try{setDevices(await getDevices())}catch{setError("Unable to load devices.")}finally{setLoading(false)}}useEffect(()=>{void load()},[]);const filtered=useMemo(()=>devices.filter(d=>(status==='all'?(true):status==='unknown'?!d.status:d.status===status)&&`${d.name} ${d.type} ${d.serialNumber}`.toLowerCase().includes(query.toLowerCase())),[devices,query,status]);const limit=checkDeviceLimit(devices.length);return <div className="space-y-5"><header className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><PageTitle>Devices</PageTitle><BodyText className="mt-1">Manage connected devices and inspect operational state.</BodyText></div>{can('device.manage')&&(limit.allowed?<Link to="/app/devices/add"><Button leadingIcon={<Plus size={15}/>}>Add device</Button></Link>:<Button disabled title={`Plan limit: ${limit.limit}`}>Device limit reached</Button>)}</header><div className="grid gap-3 sm:grid-cols-[1fr_180px]"><Input aria-label="Search devices" placeholder="Search devices" value={query} onChange={e=>setQuery(e.target.value)}/><Select aria-label="Filter by status" value={status} onChange={e=>setStatus(e.target.value)}><option value="all">All states</option><option value="online">Online</option><option value="offline">Offline</option><option value="maintenance">Maintenance</option><option value="inactive">Inactive</option><option value="unknown">Unknown</option></Select></div>{loading?<LoadingState label="Loading devices..."/>:error?<ErrorState description={error} retry={load}/>:devices.length===0?<EmptyState title="No devices connected" description="Register a device to start receiving telemetry." action={can('device.manage')&&limit.allowed?<Link to="/app/devices/add"><Button size="small">Add device</Button></Link>:undefined}/>:<><div className="hidden md:block"><DataTable rows={filtered} getRowKey={d=>d.id??d.serialNumber} caption="Organization devices" empty="No devices match this search." columns={[{key:'name',header:'Device',render:d=><div><p className="font-medium text-[var(--ds-text)]">{d.name}</p><p className="text-[11px] text-[var(--ds-text-subtle)]">{d.serialNumber}</p></div>},{key:'type',header:'Type',render:d=>d.type},{key:'status',header:'Status',render:d=><DeviceStatus status={d.status}/>},{key:'seen',header:'Last seen',render:d=>d.lastSeen?new Date(d.lastSeen).toLocaleString():'Unavailable'},{key:'connection',header:'Connection',render:d=><span className="uppercase">{d.protocol||'Unknown'}</span>},{key:'action',header:'',align:'right',render:d=><Link to={`/app/devices/${d.id}`}><Button size="compact" variant="ghost" aria-label={`Open ${d.name}`}><ExternalLink size={14}/></Button></Link>}]} /></div><div className="grid gap-3 md:hidden">{filtered.map(device=><DeviceCard key={device.id} device={device}/>)}</div></>}</div>}
