import {useParams,useSearchParams} from "react-router-dom";import AnalyticsHeader from "../../components/analytics/layout/AnalyticsHeader";import TelemetryPanel from "../../components/telemetry/layout/TelemetryPanel";export default function MetricAnalytics(){const {metric}=useParams();const [params]=useSearchParams();return <div className="space-y-5"><AnalyticsHeader title={metric??'Metric analytics'} description="Trend visualization, backend statistics, and historical analysis."/><TelemetryPanel fixedMetric={metric} fixedDeviceId={params.get('deviceId')??undefined}/></div>}
