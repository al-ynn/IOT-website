import {useEffect,useState} from "react";import {getPermissions} from "../../services/permission.service";import type {Permission} from "../../types/permission";import {useAuth} from "../../hooks/useAuth";import OrganizationHeader from "../../components/organization/layout/OrganizationHeader";import OrganizationTabs from "../../components/organization/layout/OrganizationTabs";import PermissionList from "../../components/organization/roles/PermissionList";import {Badge,Card,CardContent,CardHeader,EmptyState,ErrorState,LoadingState} from "../../components/ui";
export default function OrganizationRoles(){const {user}=useAuth();const [permissions,setPermissions]=useState<Permission[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{getPermissions().then(setPermissions).catch(()=>setError('Unable to load permissions.')).finally(()=>setLoading(false))},[]);return <div className="space-y-5"><OrganizationHeader name="Roles and permissions" description="Read-only visibility into your current organization access."/><OrganizationTabs/><Card><CardHeader title="Current role" description="Role mutation and a complete role matrix are not exposed by the backend."/><CardContent><Badge>{user?.role??'Unavailable'}</Badge></CardContent></Card>{loading?<LoadingState label="Loading permissions..."/>:error?<ErrorState description={error}/>:permissions.length?<PermissionList permissions={permissions}/>:<EmptyState title="No permissions returned" description="Your account has no visible organization permissions."/>}</div>}
