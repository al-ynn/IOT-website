import {useParams,useSearchParams} from "react-router-dom";import TelemetryHeader from "../../components/telemetry/layout/TelemetryHeader";import TelemetryPanel from "../../components/telemetry/layout/TelemetryPanel";export default function TelemetryMetric(){const {metric}=useParams();const [params]=useSearchParams();const deviceId=params.get('deviceId')??undefined;return <div className="space-y-5"><TelemetryHeader title={metric??'Metric telemetry'} description="Metric-focused telemetry history and statistics."/><TelemetryPanel fixedMetric={metric} fixedDeviceId={deviceId}/></div>}
