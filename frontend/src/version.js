// Build version marker — kept as plain JS so JS-only tooling (linters,
// bundler probes) always has at least one source file to operate on.
export const APP_BUILD_VERSION = "2.0.0-redesign";
export default APP_BUILD_VERSION;
